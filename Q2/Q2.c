#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
char a[20];
char b[20];
char c[20];
gets(a);
gets(b);
gets(c);
printf("%s\n%s\n%s",a,b,c);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}

